name: deploy-app

# Code-only deploy path: triggered when app/** changes (dashboard frontend) or
# manually. Per the architecture doc this must NOT rebuild data — it retrieves
# and re-validates the last-known-good published snapshot from the Firestore
# archive (deterministic record contract, R4-5.b) and redeploys app/ with that
# data, so a CSS/JS fix ships without waiting on/re-running the daily pipeline.
#
# Shares a single `production` release lock with daily.yml (see `concurrency`
# below) so the two workflows never race a Cloudflare Pages deploy or a
# Firestore archive write against each other.

on:
  push:
    branches: [main]
    paths:
      - "app/**"
  workflow_dispatch:
    inputs:
      allow_bootstrap:
        description: "Allow deploy without a prior successful deployment (rollback target). Only for the very first production deploy."
        required: false
        type: boolean
        default: false

# Least privilege by default; `id-token: write` is granted ONLY on the
# resolve-last-known-good job below (the one GCP/WIF-auth job in this
# workflow), never on any other job.
permissions:
  contents: read

# Shared across BOTH workflows (this file and daily.yml). GitHub Actions
# concurrency groups are workflow-agnostic — a group name is global to the
# repo — so naming them identically here and in daily.yml means only one
# release (whichever pipeline it came from) is ever in flight, and the lock
# is held for the whole job (select -> validate -> deploy -> smoke ->
# rollback/success), not released early at the `wrangler deploy` step.
concurrency:
  group: forwardguidex-production-deployment
  cancel-in-progress: false

env:
  CF_PROJECT_NAME: fgx-dashboard
  # Set repo variable APP_HOST to your live dashboard URL (e.g. the exact
  # fgx-dashboard.pages.dev subdomain, or https://app.forwardguidex.com once the
  # custom domain is added). Falls back to the default pages.dev name.
  APP_HOST: ${{ vars.APP_HOST || 'https://fgx-dashboard.pages.dev' }}

jobs:
  # ---------------------------------------------------------------------
  # Job 1 — resolve the deterministic last-known-good snapshot.
  #
  # Source order per the plan is "Firestore archive -> GH artifact ->
  # currently-deployed". This job implements the primary source (Firestore
  # archive) by calling `fwdx retrieve-lkg`, a thin wrapper over the
  # unit-tested `publish.retrieve_last_known_good()` + `write_last_known_good()`.
  #
  # It used to paraphrase that logic as an inline Firestore REST query, and the
  # paraphrase drifted: the hand-built manifest set `generated_at` from the
  # archive record's `deployed_at`, so every code-only deploy published a
  # snapshot claiming to be fresher than its data. The hashes still matched, so
  # `fwdx validate` passed it. One implementation, one contract — and the
  # validator now cross-checks `manifest.generated_at` against `meta` as well.
  #
  # The Python resolver also scans up to `publish._RETRIEVE_SCAN_LIMIT` records
  # in deployed_at-descending order and applies the full record contract
  # (SMOKE_TESTED + is_demo=false + hex64 artifact + schema_version 1 +
  # complete provenance), where the REST query took `limit: 1` and trusted it —
  # so a single malformed record at the head of the archive no longer blocks
  # every code deploy. It orders on one field, so it needs no composite index.
  #
  # The GH-artifact and currently-deployed fallbacks are still NOT implemented:
  # if the archive yields nothing, this job fails closed rather than guessing
  # ("fail (never DEMO) if none" per R4-5.b).
  # ---------------------------------------------------------------------
  resolve-last-known-good:
    name: Resolve last-known-good snapshot (Firestore archive, read-only WIF)
    runs-on: ubuntu-latest
    environment: production
    timeout-minutes: 15
    permissions:
      contents: read
      id-token: write # ONLY this job needs id-token:write, for WIF -> GCP.
    outputs:
      artifact_sha256: ${{ steps.resolve.outputs.artifact_sha256 }}
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
          fetch-depth: 1
      - name: Set up Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.11"

      - name: Install forwardguidex (core + publish extra, hash-pinned)
        # `publish.lock`, not `core.lock`: the reader path goes through
        # firebase-admin, exactly like daily.yml's publish job. Same
        # hash-pinned, wheel-only install contract — see that job for why.
        run: |
          set -euo pipefail
          pip install --require-hashes --only-binary=:all: -r requirements/publish.lock
          pip install --no-deps -e .
          pip check

      - name: Authenticate to GCP (WIF, reader service account)
        id: auth
        # No `token_format`/`access_token_scopes`: firebase-admin reads the
        # Workload Identity Federation credential file this action writes (still
        # keyless — never a service-account key) as ADC. The *read-only*
        # guarantee comes from IAM — WIF_READER_SA should be bound to
        # roles/datastore.viewer only, see the deploy runbook — never from a
        # scope string.
        uses: google-github-actions/auth@c200f3691d83b41bf9bbd8638997a462592937ed # v2.1.13
        with:
          workload_identity_provider: ${{ secrets.WIF_PROVIDER }}
          service_account: ${{ secrets.WIF_READER_SA }}

      - name: Resolve the newest SMOKE_TESTED, non-demo snapshot from the archive
        id: resolve
        env:
          GOOGLE_CLOUD_PROJECT: ${{ vars.GOOGLE_CLOUD_PROJECT }}
        run: |
          set -euo pipefail
          if [ -z "${GOOGLE_CLOUD_PROJECT:-}" ]; then
            echo "::error::GOOGLE_CLOUD_PROJECT is not set (repo/environment variable). Cannot resolve last-known-good."
            exit 1
          fi
          # Exits 1 when no archived record satisfies the contract. It verifies
          # sha256(payload_json) == metadata.artifact_sha256 before writing, so
          # `resolved/` is either a coherent bundle or absent.
          fwdx retrieve-lkg --out-dir resolved
          SHA=$(python3 -c "import json; print(json.load(open('resolved/latest.json'))['artifact_sha256'])")
          echo "artifact_sha256=$SHA" >> "$GITHUB_OUTPUT"

      - name: Upload resolved snapshot for the deploy job
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: last-known-good-snapshot
          path: resolved/
          retention-days: 7
          if-no-files-found: error

  # ---------------------------------------------------------------------
  # Job 2 — re-validate, Direct Upload, authenticated smoke test, rollback.
  # ---------------------------------------------------------------------
  deploy:
    name: Re-validate + deploy app/ (Cloudflare Pages Direct Upload) + smoke test
    needs: resolve-last-known-good
    runs-on: ubuntu-latest
    environment: production
    timeout-minutes: 20
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
          fetch-depth: 1
      - name: Set up Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.11"

      - name: Install forwardguidex (core, hash-pinned, for `fwdx validate`)
        # P1.2: hash-pinned, wheel-only install. `pip install -e .` resolved open
        # ranges (pandas>=2.0, duckdb>=0.10, yfinance>=0.2.40) fresh on every run,
        # so a compromised or simply broken upstream release landed straight in
        # production with no review. requirements/*.lock are generated by
        # scripts/lock-deps.sh for cp311 / manylinux — the exact CI target.
        run: |
          set -euo pipefail
          pip install --require-hashes --only-binary=:all: -r requirements/core.lock
          pip install --no-deps -e .
          pip check

      - name: Download resolved last-known-good snapshot
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: last-known-good-snapshot
          path: resolved/

      - name: Re-validate the resolved snapshot (fail-closed)
        run: |
          set -euo pipefail
          SNAP=$(ls resolved/snapshot.*.json | head -n1)
          echo "Re-validating $SNAP"
          fwdx validate "$SNAP"

      - name: Assert is_demo == false (explicit deploy-time gate, belt-and-suspenders)
        run: |
          set -euo pipefail
          python3 - <<-'PYEOF'
          	import glob
          	import json
          	import sys

          	snap_path = glob.glob("resolved/snapshot.*.json")[0]
          	with open(snap_path, encoding="utf-8") as f:
          	    snap = json.load(f)
          	meta = snap.get("meta", {})
          	if meta.get("is_demo") is not False:
          	    sys.exit(f"::error::{snap_path} has meta.is_demo={meta.get('is_demo')!r}; refusing to deploy demo data to production.")
          	if meta.get("schema_version") != 1:
          	    sys.exit(f"::error::{snap_path} has unsupported meta.schema_version={meta.get('schema_version')!r}.")
          	print(f"OK: {snap_path} is_demo=false, schema_version=1.")
          	PYEOF

      - name: Stage resolved snapshot into app/data/ (prod files alongside demo files)
        run: |
          set -euo pipefail
          cp resolved/snapshot.*.json resolved/latest.json app/data/
          ls -la app/data/

      # P0.3 — fail-closed rollback-target lookup (bounded retries, strict
      # JSON validation, bootstrap only via explicit workflow_dispatch input).
      - name: Capture current live deployment (pre-deploy, for rollback target)
        id: pre
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          ALLOW_BOOTSTRAP: ${{ github.event.inputs.allow_bootstrap || 'false' }}
        run: |
          set -euo pipefail
          resp_file=$(mktemp)
          curl -sS --fail-with-body \
               --retry 3 --retry-delay 4 --retry-connrefused \
               --retry-all-errors \
               --connect-timeout 10 --max-time 45 \
               -H "Authorization: Bearer ${CLOUDFLARE_API_TOKEN}" \
               -o "$resp_file" \
               "https://api.cloudflare.com/client/v4/accounts/${CLOUDFLARE_ACCOUNT_ID}/pages/projects/${CF_PROJECT_NAME}/deployments?per_page=25"
          python3 - "$resp_file" <<-'PYEOF'
          	import json
          	import os
          	import re
          	import sys
          	path = sys.argv[1]
          	try:
          	    body = json.load(open(path, encoding="utf-8"))
          	except Exception as e:  # noqa: BLE001
          	    sys.exit(f"::error::rollback lookup: unparseable JSON ({e}).")
          	if not isinstance(body, dict) or body.get("success") is not True:
          	    sys.exit(f"::error::rollback lookup: Cloudflare API success!=true ({body.get('errors') if isinstance(body, dict) else None}).")
          	result = body.get("result")
          	if not isinstance(result, list):
          	    sys.exit("::error::rollback lookup: response.result is not an array.")
          	prev_id = ""
          	id_pat = re.compile(r"^[0-9a-fA-F-]{8,64}$")
          	for d in result:
          	    if not isinstance(d, dict):
          	        continue
          	    if d.get("environment") != "production":
          	        continue
          	    stage = (d.get("latest_stage") or {}).get("status")
          	    if stage != "success":
          	        continue
          	    did = d.get("id") or ""
          	    if not id_pat.match(did):
          	        continue
          	    prev_id = did
          	    break
          	if not prev_id:
          	    if os.environ.get("ALLOW_BOOTSTRAP", "").lower() == "true":
          	        print("::warning::rollback lookup: no previous successful production deployment; bootstrap explicitly allowed.")
          	    else:
          	        sys.exit("::error::rollback lookup: no previous successful production deployment found. Re-run with workflow_dispatch input allow_bootstrap=true only for the first deploy.")
          	with open(os.environ["GITHUB_OUTPUT"], "a", encoding="utf-8") as f:
          	    f.write(f"deployment_id={prev_id}\n")
          	print(f"Previous successful production deployment: {prev_id or 'none (bootstrap)'}")
          	PYEOF

      - name: Deploy app/ to Cloudflare Pages (Direct Upload)
        id: deploy_cf
        uses: cloudflare/wrangler-action@da0e0dfe58b7a431659754fdf3f186c529afbe65 # v3.14.1
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          command: pages deploy app --project-name=fgx-dashboard --branch=main

      # P0.2 — Fail-closed smoke test against the UNIQUE deployment URL.
      # Extracted into a shared script (.github/workflows/smoke.py) reused by
      # daily.yml so both pipelines converge on one contract.
      - name: Extract snapshot name for smoke test
        id: snap_meta
        run: |
          set -euo pipefail
          NAME=$(python3 -c "import json,glob,sys; p=glob.glob('resolved/snapshot.*.json')[0]; print(p.rsplit('/',1)[-1])")
          SHA=$(python3 -c "import json; print(json.load(open('resolved/latest.json'))['artifact_sha256'])")
          echo "snapshot_name=$NAME" >> "$GITHUB_OUTPUT"
          echo "artifact_sha256=$SHA" >> "$GITHUB_OUTPUT"

      - name: Verify deployment (live bytes + auth gate + /api/chat)
        id: smoke
        continue-on-error: true
        env:
          APP_HOST: ${{ env.APP_HOST }}
          UNIQUE_DEPLOY_URL: ${{ steps.deploy_cf.outputs.deployment-url }}
          DASHBOARD_PASSWORD: ${{ secrets.DASHBOARD_PASSWORD }}
          EXPECTED_ARTIFACT_SHA256: ${{ steps.snap_meta.outputs.artifact_sha256 }}
          EXPECTED_SNAPSHOT_NAME: ${{ steps.snap_meta.outputs.snapshot_name }}
        run: |
          set -euo pipefail
          python3 .github/workflows/smoke.py 2>&1 | tee smoke-test.log
      - name: Upload smoke test log
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: smoke-test-log-${{ github.run_id }}-${{ github.run_attempt }}
          path: smoke-test.log
          retention-days: 30
          if-no-files-found: warn

      # --- Rollback path (smoke failed) ---------------------------------
      # The concurrency lock (workflow-level, see top of file) stays held
      # through all of this: we do not release it early, and we do not set
      # cancel-in-progress, so no other production deploy/publish can start
      # until this job (rollback included) finishes.
      - name: Rollback to previous successful deployment (smoke failed)
        if: always() && steps.smoke.outcome == 'failure'
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          PREV_DEPLOYMENT_ID: ${{ steps.pre.outputs.deployment_id }}
        run: |
          set -euo pipefail
          if [ -z "${PREV_DEPLOYMENT_ID:-}" ]; then
            echo "::error::Smoke failed but no previous successful deployment exists (bootstrap-run). Manual intervention required."
            exit 1
          fi
          echo "Rolling back Cloudflare Pages project fgx-dashboard to deployment $PREV_DEPLOYMENT_ID"
          curl -sS --fail-with-body \
               --retry 3 --retry-delay 4 --retry-connrefused --retry-all-errors \
               --connect-timeout 10 --max-time 60 \
               -X POST \
               -H "Authorization: Bearer ${CLOUDFLARE_API_TOKEN}" \
               -H "Content-Type: application/json" \
               "https://api.cloudflare.com/client/v4/accounts/${CLOUDFLARE_ACCOUNT_ID}/pages/projects/fgx-dashboard/deployments/${PREV_DEPLOYMENT_ID}/rollback" \
               -o rollback-response.json
          python3 -c "import json,sys; b=json.load(open('rollback-response.json')); sys.exit(0 if b.get('success') is True else 1)"

      - name: Verify rollback (re-run Access + hash checks only)
        if: always() && steps.smoke.outcome == 'failure'
        env:
          APP_HOST: ${{ env.APP_HOST }}
          DASHBOARD_PASSWORD: ${{ secrets.DASHBOARD_PASSWORD }}
        run: |
          set -euo pipefail
          python3 - <<-'PYEOF'
          	import base64
          	import hashlib
          	import json
          	import os
          	import sys
          	import time
          	import urllib.error
          	import urllib.request

          	host = os.environ["APP_HOST"]
          	headers = {
          	    "Authorization": "Basic " + base64.b64encode(("ci:" + os.environ["DASHBOARD_PASSWORD"]).encode()).decode(),
          	    "User-Agent": "Mozilla/5.0 (X11; Linux x86_64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/122.0.0.0 Safari/537.36",
          	}
          	cache_bust = f"?_smoke_rollback={int(time.time())}"

          	def fetch(path):
          	    req = urllib.request.Request(host + path + cache_bust, headers=headers)
          	    try:
          	        with urllib.request.urlopen(req, timeout=30) as resp:
          	            return resp.status, resp.read()
          	    except urllib.error.HTTPError as e:
          	        return e.code, e.read()

          	status, _ = fetch("/")
          	if status != 200:
          	    sys.exit(f"::error::Post-rollback GET / -> HTTP {status}. Rollback did not restore service.")

          	status, body = fetch("/data/latest.json")
          	if status != 200:
          	    sys.exit(f"::error::Post-rollback GET /data/latest.json -> HTTP {status}.")
          	manifest = json.loads(body)

          	status, snap_body = fetch(f"/data/{manifest['snapshot']}")
          	if status != 200 or hashlib.sha256(snap_body).hexdigest() != manifest["artifact_sha256"]:
          	    sys.exit("::error::Post-rollback snapshot hash verification failed.")

          	print("OK: rollback verified — previous deployment is live and internally consistent.")
          	PYEOF

      - name: Alert — smoke test failed, rollback performed
        if: always() && steps.smoke.outcome == 'failure'
        run: |
          echo "::error::Deploy smoke test FAILED for ${APP_HOST}. Rolled back to the previous successful Cloudflare Pages deployment (id: ${{ steps.pre.outputs.deployment_id }}). See the smoke-test-log-${{ github.run_id }}-${{ github.run_attempt }} artifact for details. This job is intentionally failing so GitHub surfaces a run failure (Actions notification / status check) as the alert; wire a dedicated channel (Slack/email/PagerDuty webhook) here if stronger paging is needed."
          exit 1
